# GitHub Governance Open PR Evidence

This directory contains pull-request evidence for the GitHub governance open
pull-request release-readiness guard. The PR adds an `open_pull_requests` summary
to the governance sidecar, validates that summary when release bundles consume
it, and documents the current-PR allowlist flag.
