# Standalone Benchmark Evidence Folders PR Evidence

This folder records traceability evidence for PR #431, which moved sanitized
Databricks benchmark artifacts into standalone human-readable benchmark folders
under `benchmarks/`.

The JSON sidecar is machine-checkable `document_kv.pr_evidence.v1` evidence for
release-bundle traceability. It is not a benchmark artifact itself.
