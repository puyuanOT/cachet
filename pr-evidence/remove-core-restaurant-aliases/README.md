# Remove Core Restaurant Aliases PR Evidence

This folder records traceability evidence for PR #442, which removes
restaurant-named compatibility aliases from Cachet's core runtime model,
planner, manifest, service, and root export surfaces.

The JSON sidecar is machine-checkable `document_kv.pr_evidence.v1` evidence for
release-bundle traceability. It is not benchmark output; benchmark reports live
under `benchmarks/`.
