# Poetry Metadata Cleanup PR Evidence

This folder records traceability evidence for PR #435, which removes duplicate
Poetry identity metadata now that PEP 621 `[project]` metadata is authoritative.

The JSON sidecar is machine-checkable `document_kv.pr_evidence.v1` evidence for
release-bundle traceability. It is not benchmark evidence or runtime connector
configuration.
