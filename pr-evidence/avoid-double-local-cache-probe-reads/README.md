# Avoid Double Local Cache Probe Reads Evidence

This directory contains the pull-request evidence sidecar and validation record
for reducing duplicate local-disk reads during batched chunk-cache materialization.
