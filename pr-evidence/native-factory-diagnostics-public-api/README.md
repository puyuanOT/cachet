# Native Factory Diagnostics Public API Evidence

This directory contains the pull-request evidence sidecar for exposing native
probe factory diagnostics record metadata and the JSON writer through the public
Document KV Cache, Cachet, and legacy compatibility facades.
