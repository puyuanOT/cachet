# Current Benchmark Results Index PR Evidence

This folder records traceability evidence for PR #511, which added
`benchmarks/current/README.md` as the human-facing current benchmark index and
kept `pr-evidence/` out of the benchmark report surface.

The JSON sidecar is `document_kv.pr_evidence.v1` release-bundle traceability.
It is not benchmark output; benchmark reports live under `benchmarks/`.
