# Native Engine Benchmark Index

Native engine probes are integration checks, not serving-latency benchmark
rows. The [benchmark root](../) is the governed current-state surface for the
frozen vLLM 0.27.1 campaign; native probes do not populate its result tables.

Historical native connector probe records were removed from `benchmarks/`.
No native-engine benchmark result evidence is currently published here.
