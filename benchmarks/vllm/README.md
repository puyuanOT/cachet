# vLLM Benchmark Index

The active target is the frozen vLLM 0.27.1 publication campaign described in
the [benchmark root](../). Baseline and Vanilla KV cover 8k, 16k, and 32k with
closed-loop concurrency 1, 2, and 4 across five matched deployment blocks.

No pre-reset numeric result is carried forward. Implemented latency, resource,
ablation, and full-score cells remain pending unless the sealed, sanitized
campaign report and exact passing publication-gate pair are committed; the
benchmark root is the canonical current-state surface.
