# Storage Benchmark Index

Storage appears in the paper-style appendix as a storage-tier ablation and as
resource-utilization fields. See [`../current/`](../current/). Blank numeric
cells mean the target serving benchmark has not measured that metric yet.

Existing storage-reader evidence moved to:

| Appendix result | Status |
| --- | --- |
| [`../appendix/existing-results/storage-g6-l4-reader-throughput/`](../appendix/existing-results/storage-g6-l4-reader-throughput/) | Memory, disk, and Unity Catalog reader throughput over a 256 MiB workload |

The existing storage evidence is not a model-serving latency benchmark and is
not a memory-consumption measurement.
