# SGLang Benchmark Index

The current main benchmark table is fixed to vLLM. SGLang appears only in the
serving-platform ablation in the [benchmark root](../), with blank cells until
a matching Q4-weight + Q8-document-KV run exists.

Historical SGLang smoke attempts remain outside `benchmarks/` under
[`../../docs/release-ops/benchmark-archive/`](../../docs/release-ops/benchmark-archive/)
for maintainer debugging.
