# SGLang Benchmark Index

The main reset campaign targets vLLM 0.27.1. Cachet's Q8 pre-RoPE SGLang
serving path is not implemented, so the SGLang main-table and platform-ablation
cells are explicitly `N/A (Q8 pre-RoPE serving path not implemented)`.

Integration probes do not substitute for serving-latency or full-score results.
