# Source Layout

This folder contains importable Python packages for the document KV-cache library.

- `document_kv_cache/` is the public import facade and CLI namespace for new
  users.
- `restaurant_kv_serving/` contains the current implementation and remains
  packaged as a compatibility layer for existing Databricks benchmark jobs.
