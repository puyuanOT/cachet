# Dependency Freshness Evidence

This directory contains durable Cachet dependency-freshness evidence. It is
separate from `benchmarks/`, which holds benchmark reports, and from
`pr-evidence/`, which holds pull-request traceability sidecars.

Use [`current/`](current/) for the latest generated
`document_kv.dependency_freshness.v1` record.

