# Sort Batched Disk Range Reads Evidence

This directory contains the pull-request evidence sidecar and validation record
for sorting same-shard disk range reads by byte offset while preserving caller
payload order.
