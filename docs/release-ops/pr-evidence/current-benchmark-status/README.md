# Current Benchmark Status PR Evidence

This folder records traceability evidence for PR #436, which adds a
human-readable current Databricks benchmark snapshot outside `pr-evidence/` and
ignored local `databricks-runs/` output.

The JSON sidecar is machine-checkable `document_kv.pr_evidence.v1` evidence for
release-bundle traceability. It is not benchmark evidence or runtime connector
configuration.
