# Dependency Freshness Evidence PR Evidence

This folder records traceability evidence for PR #513, which adds
dependency-freshness release evidence and wires it into strict V1 release
bundles.

The JSON sidecar is `document_kv.pr_evidence.v1` release-bundle traceability.
It is not benchmark output; human-readable benchmark reports live under
`benchmarks/`.
