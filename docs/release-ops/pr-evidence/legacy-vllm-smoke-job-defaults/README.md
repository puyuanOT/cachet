# Legacy vLLM Smoke Job Defaults Evidence

This folder records the PR traceability evidence for isolating the
`restaurant_kv_serving.databricks_vllm_smoke_job` compatibility facade from
import-order monkeypatches of the public document namespace.

The change applies the Refactor skill, preserves normal public class identity
for pristine imports, and includes GPT-5.5 review evidence before merge.
